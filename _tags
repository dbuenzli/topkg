true : bin_annot, safe_string, package(bytes), package(result)

<src> : include

# Remove warning about String.uppercase we still support 4.01
<src/topkg_conf_ocaml.ml> : warn(-3)

<src-{care,bin}> : include
<src-{care,bin}/*> : package(opam-lib), package(cmdliner), \
  package(astring), package(rresult), package(fpath), \
  package(fmt), package(fmt.tty), package(fmt.cli), \
  package(logs), package(logs.fmt), package(logs.cli), \
  package(bos), package(bos.setup)

<test> : include
<support> : include