true : bin_annot, safe_string, package(bytes), package(result)

<src> : include

<src-{care,bin}> : include
<src-{care,bin}/*> : package(opam-lib), package(cmdliner), \
                     package(fmt.cli), package(logs.cli), \
                     package(bos.setup), package(webbrowser), \
                     package(webbrowser.cli)

<test> : include
<support> : include